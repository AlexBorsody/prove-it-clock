# Task log — 2026-09-26

## Promise-linked "Recently Happened" news feed — SHIPPED by Codex

Update 03:20 EDT: Codex shipped this independently as commit 4d43a65
("Add promise stats and automatically linked project news") before the spec
below was finished. The spec in `~/workspace/your_files/prove-value-promise-news-spec.md`
is marked superseded with the shipped implementation recorded.

Shipped: `promise-news.tsx` + `promise-stats.tsx` components,
`lib/promise-context.ts` keyword matcher (2 distinct topic words, project name
never links alone), mounted on `/projects/[slug]`, test at
`app/scripts/promise-context.test.ts`. Source is the existing
`/api/v1/mentions/[slug]` Google News RSS feed, not a new RSS route.

## Original spec (superseded)

Alex approved the promise-linked news concept (not a CoinGecko clone). Spec v1:
`~/workspace/your_files/prove-value-promise-news-spec.md`.

Summary for the build:

- Source: public RSS (CoinDesk, The Block, Decrypt, CoinTelegraph, Bitcoin
  Magazine, Ethereum Foundation blog). CryptoPanic free API is dead since
  2026-04-01; no keys, zero cost.
- Route: `GET /api/news/[slug]`, server-side RSS fetch, 30-min revalidate,
  title dedupe (Jaccard >= 0.8), newest 15 per project.
- Coin filter: per-project alias set matched against headline + summary.
- Promise matching v1: keyword scoring on lineage-slug tokens (x2) plus criteria
  text (x1), threshold 0.25, tag lineage ids on each item.
- Display: "Recently Happened" section on `/projects/[slug]` below the power
  grid. Items show time, headline link, source, and promise chips that scroll to
  the promise row. Toggle: linked-only vs all. No sentiment voting on items.
- Hard rule: news never changes promise state; it only points at promises for
  the next research pass.
- v2 (parked): embedding cosine match reusing the index's pinned embedding
  model; researcher inbox per project.

Placement on the project page to be confirmed with Alex before build.

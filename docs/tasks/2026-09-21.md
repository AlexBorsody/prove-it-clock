# Task doc — 2026-09-21 (Mon)

Read `docs/strategy.md` first if anything here is unclear.

## Done today
- Visual QA of production: `/`, `/projects/btc`, `/projects/usdt`, `/methodology` — all pass.
- Footer bug fixed: site-wide footer claimed v0.3.0; now v0.2.0 everywhere. Pushed (remote `75ccf1da`), deploy verified live.

## Building today
- Embeddable timeline widget (`implementation.md` §7): server-rendered SVG
  route `app/embed/projects/[slug]/timeline`, light/dark themes, iframe-friendly,
  copy-paste snippet on scored project pages.

## Queued (this week, in order)
1. Trading-card layout + per-category chart lines (`implementation.md` §10):
   project page becomes the Pokémon/Marvel-style card with stat bars per v0.2.0
   category; ScoreTimeline gains per-category lines feeding the overall line.
   Starts after the widget lands (both touch project pages + ScoreTimeline).
2. Daily historical-data cadence (`implementation.md` §11): loader script +
   score.ts version fix + daily cron. The alerts feed (§9) needs this running.
3. SEO structured-data pass (`implementation.md` §8).
4. Promise-gap alerts feed (`implementation.md` §9).

## Blocked on Alex
- Telegram broadcast channel (needs his Telegram account).
- Next scoring-update cadence (his methodology call).
- Publisher disclosures unconfirmed (Dash/BAT/AVAX/LINK positions).

## Notes
- Rules that keep biting: v0.3.0 is speculative — never in active output.
  Unscored projects show explicit unavailable state, never zeros.
  Strategy stays in `strategy.md` — this doc stays technical.

## Standing: build fast
- Alex 2026-09-21: ease up on tests, just build. Gates are compiles + deploys +
  spot-check. No heavy validation passes for now.

## §7 widget — shipped 2026-09-21
- Remote e9e8b501, live on production. `/embed/projects/[slug]/timeline`,
  light/dark, iframe-friendly, unscored renders explicit unavailable state.
  Embed snippet on scored project pages.
- Next up: §10 trading-card layout + per-category chart.

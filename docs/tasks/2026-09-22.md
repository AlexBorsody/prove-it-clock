# Task log — 2026-09-22

## Current plan consolidated

Implementation.md now organizes the agreed concept once: product, heart rules,
illustrative XRP/BAT meters, minimal UI, the distinction between hearts and
price, and three remaining decisions (rewards/capacity, decay, valuation).
README and strategy now consistently distinguish maximum hearts from the
starting allowance. The entries below preserve the discussion history; they
do not override the current implementation plan. Documentation only.


## Latest direction — graph with a starting allowance

Alex clarified the model: supporting factors (team, utility, impact of each
promise and overall vision) provide a modest starting boost, like initial health
points. Meaningful delivery supports or raises the value graph; sustained
non-delivery wears the unearned allowance down, eventually to zero if no token
value is demonstrated. XRP is the evidence-tested non-delivery example.

No separate clock UI or recurring reputation bonus. Starting inputs are counted
once; delivered utility is not double-counted. Updated implementation, README,
and strategy. The allowance, decay endpoint, and economic mapping still need
definition. Earlier scope decisions below are historical. No code or data changed.

## Scope reset — Alex

Alex narrowed the product to promises kept and potential of the promise, with
fulfillment versus time running out leading toward justified market cap. Team,
utility, and other factors support potential; they are not separate product
tracks. Replaced implementation.md with that scope, archived the prior plan
including Alex’s additions, and aligned README/strategy. Earlier queue and
formula references below are historical. No code, scores, or DB data changed.

## Completed — Codex

- Read all repo Markdown and reviewed the provider/pipeline/scoring paths, schema and seed generators, DB readers, APIs, pages, and shared chart renderer.
- Backend ownership recorded in [implementation.md](../implementation.md): architecture, schemas, ingestion, and frontend/API/DB contracts alongside Muse.
- Consolidated architecture findings into the technical plan. Separated current card/timeline and daily-cadence deliverables from future milestone, Promise Score, Context, and AI work.
- During review, HEAD advanced to `11b68af`, merging the snapshot loader (`ef2d1ed`). Re-reviewed the loader and updated the plan: harden the existing implementation rather than build another. No production write was run.
- Corrected stale claims: card/StatBar already exist; 84 active rows are one date; pipeline artifacts are overwriteable; local fallback has no active scored history; the projects API makes upstream calls.
- Removed repeated onboarding prose, obsolete section references, and superseded queues. README now points to one planning surface and describes the actual local setup. Prior decisions remain in the September 21 log.

## Verification and evidence

- GitHub connector confirmed read/push/admin access to `AlexBorsody/prove-it-clock`; checkout is writable.
- Production history API returned successfully for BTC, ETH, XRP, SOL, ADA, LINK, and USDT. Six projects each returned 14 v0.2.0 rows dated September 21, with earlier v0.1.0 history. USDT had no score series. Evidence Confidence had no dedicated series.
- Installed exact lockfile dependencies with lifecycle scripts disabled. Typecheck, production build, and existing event validation passed during onboarding, without DB credentials. Docs-only cleanup does not require rerunning those checks.
- Isolated in-memory scorer probes: changing only market cap changed BTC Reflexivity Risk from 3.6 to 3.1; measured zero fees normalized to null; a future fulfilled milestone contributed 5 to milestone velocity. No pipeline or snapshot writes were performed. These findings need explicit methodology decisions.
- Reviewed generator provenance: v0.2.0 carries September 19 dimension scores into September 21 rows, and uses two-decimal Outlook rounding versus the engine's one decimal.
- Typecheck also passed after the loader merge. Markdown code fences, all nine local links, and `git diff --check` passed after cleanup.

## Local runtime note

Node/npm were absent from the shell PATH. Bundled Node:
`/Users/alexborsody/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node`.
Temporary npm bootstrap: `/tmp/prove-it-clock-npm/package/bin/npm-cli.js`
(may disappear on temp cleanup). Configure a normal Node/npm PATH for routine
work. From `app/`, `node --import tsx scripts/events-check.ts` avoids the `tsx`
CLI IPC socket restriction in this sandbox.

## Remaining access and handoff

Direct Supabase and Vercel management access is unverified: no project env,
Vercel link, or standard Supabase/Vercel CLI configuration was found in checked
locations. Obtain the existing local credential/project setup, then inspect
schema/grants read-only against migration 001. Never place credentials here.

Read the implementation plan's findings before scheduling the existing daily loader.
Resolve the scoring conflicts separately from plumbing changes. This review changed only Markdown; the separately merged loader was preserved.
Frozen seeds and methodology configs were not edited. No scheduler, remote push, or deployment was performed.

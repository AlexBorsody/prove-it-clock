# Code review — hearts branch (merged 2026-09-25)

**Scope:** everything Codex pushed on the `hearts` branch and merged to main
in 84c2458. **Method:** read every new/changed file; ran `test:hearts`,
`test:db`, `typecheck`, and `build` locally. **Verdict: accept.** Two nits,
no blockers.

## What was reviewed

| Area | Files | Verdict |
|---|---|---|
| Arithmetic | `app/src/lib/hearts.ts`, `hearts-config.ts` | Clean. Pure function, frozen constants match adopted rules. Validates all inputs; decides no policy (recency/reset left to reviewers). |
| Publication validation | `app/src/lib/heart-publication.ts` | Thorough. Schema versioning, ISO timestamps, draft/published gating with reviewer+policy required, HTTP(S)-only evidence URLs. |
| Publish CLI | `app/scripts/publish-hearts.ts` | Safe. `--dry-run` validates without writing; secrets from env only, never printed. |
| Read layer | `app/src/lib/heart-data.ts` | Read-only, published runs only. Falls back to service-role key if publishable key missing — fail-closed (migration revokes table access from service_role too). |
| DB migration | `db/migrations/002_heart_publications.sql` | Correct trust boundary: REVOKE ALL incl. service_role, GRANT SELECT + RLS published-only; `publish_heart_run` RPC executable by service_role only; mutation trigger makes history append-only. |
| Review pages | `app/(site)/case-studies/*`, `case-study-document.tsx`, `lib/case-studies.ts` | Allowlisted slugs (`dynamicParams=false`), `skipHtml` + safe URL transform, `robots: noindex`. No path traversal. |
| API routes | `app/api/hearts/*` | Bounded pagination, methodology required, slug format-validated, 400/503 handling. Read-only. |
| Tests | `hearts.test.ts`, `case-studies.test.ts`, `heart-database.test.ts` | Real coverage: formula boundaries, invalid-input rejection, PGlite-backed atomicity/idempotency/rollback/draft-isolation/append-only tests, SQL↔TS arithmetic agreement. |

## Mechanical verification (2026-09-25, local)

- `npm run test:hearts` — 8/8 pass.
- `npm run test:db` — 10/10 pass (after `npm install`; see nit 1).
- `npm run typecheck` — clean. `npm run build` — succeeds; all four
  case-study pages prerender as static HTML, both API routes dynamic.

## Nits (non-blocking)

1. Fresh checkouts must run `npm install` before tests/typecheck pass —
   `pglite`, `react-markdown`, `remark-gfm` were in package.json but never
   installed. Lockfile is committed, so install works.
2. The research-draft banner says "conditional calculations are review
   examples" — the worksheets now show single scores. Still true in spirit
   (no published rating), but the wording is stale.

## Follow-ups (not this task)

- The 4 open methodology questions in `docs/case-studies/review.md` still
  block any production rating. No DB write has happened; none should until
  Alex signs off the scores.
- `docs/tasks/2026-09-24.md` (Codex's log) and this file are the paper trail
  for the branch; Codex's original commits remain on the server `hearts` branch.

## Decay killed — claim-type rule adopted (2026-09-25, ~05:30)

Alex challenged time decay as arbitrary; ChatGPT steelmanned it and proposed
the third option; Alex adopted: **a heart remains earned only while the
evidence condition under which it was awarded remains true.** Milestones
permanent, ongoing claims revalidated. "Scores change because evidence
changes, not because time passes."

- `docs/hearts-algorithm.md`: GRACE_YEARS/DECAY_RATE removed; lineage types
  (milestone/ongoing) + states (open/active/lapsed/retired/superseded);
  formula has no clock. Toggle idea rejected (two methodologies, busier chart).
- Recomputed: **BAT 3/10 → 5/10** (ad loop + creators ongoing, both active;
  allowance 2 restored), **XRP 2/20 → 5/20** (ledger milestone +1, payments
  product ongoing/active +1 per Q1 2026 ODL $35B; allowance 3 restored).
  XRP ODL figures are secondary-citing-Ripple — flagged for blinded
  replication.
- `docs/case-studies/review.md`: 4 open → 3 (reset-S(t) question dissolved);
  paper tests rewritten for lapse/reactivation.
- **Codex work needed (not done):** `hearts.ts` must drop
  `yearsSinceFulfillment`/grace/decay (allowance = present-tense input);
  `heart-publication.ts` lineage schema needs `claim_type: milestone|ongoing`
  and drops `years_since_fulfillment`; migration 002 RPC recompute; tests
  rewritten. Existing tests encode the old rule and will fail until updated.
- Production DB publication still on hold: needs Supabase URL + service_role
  key from Alex, and now the new rule's code first.

## Site search — Codex, September 25

- Added unique section IDs and search metadata to project pages, leaderboards,
  comparisons, methodology, API summaries and rendered research documents.
  Fuse searches actual HTML text; results include snippets and section links.
- Shared `/api/search-index` refreshes on demand after 24 hours. Search or every
  fifth page visit checks it; failed attempts also wait 24 hours. Complete saved
  coverage survives a failed refresh. Deployment cache limits are documented in
  `implementation.md`; no scheduler or database migration was added.
- Local checks: production build/typecheck passed; search/cache and case-study
  tests passed (11). Cache regression uses installed Next 15 to exercise refresh,
  failure and fallback behavior. Repeated API calls reused one index timestamp.
- Browser checked: body query `revalidated` returned all nine matches; selecting
  XRP Lineages opened/focused its section. `borderline` opened the closed
  methodology rules panel. Mobile dropdown and arrow/Enter selection worked.
- Preview: `http://127.0.0.1:3101`. Local index has 83 sections across 17 reachable
  pages; eight project pages need local Supabase configuration, so those live
  routes still need browser acceptance with data. No hosted changes or push.
